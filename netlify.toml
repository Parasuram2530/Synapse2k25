[build]
  command = "npm run build"
  functions = "backend"
  publish = "frontend"

[functions]
  directory = "backend"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server/:splat"
  status = 200

[build.environment]
  NODE_VERSION = "18"